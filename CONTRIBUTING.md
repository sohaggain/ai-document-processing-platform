# Contributing

This is a personal portfolio project by Sohag Gain, but issues and PRs are welcome.

1. Fork the repo and create a feature branch.
2. Run `ruff check src tests` and `pytest` before opening a PR.
3. Keep changes scoped — one concern per PR.
4. Never commit real credentials, sample PII documents, or `.env` files.
